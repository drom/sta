{
  "name": "sta",
  "version": "0.4.3",
  "description": "",
  "main": "lib/",
  "bin": {
    "sta": "bin/sta.js",
    "quartus": "bin/quartus.js"
  },
  "scripts": {
    "test": "eslint src/*.js lib/*.js && mocha test",
    "prepublish": "mkdir -p bin && { echo '#!/usr/bin/env node' ; browserify --node src/sta.js ; } > bin/sta.js && { echo '#!/usr/bin/env node' ; browserify --node src/quartus.js ; } > bin/quartus.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/drom/sta.git"
  },
  "author": "Aliaksei Chapyzhenka",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/drom/sta/issues"
  },
  "homepage": "https://github.com/drom/sta#readme",
  "devDependencies": {
    "browserify": "^16.1.1",
    "eslint": "^4.18.2",
    "fs-extra": "^5.0.0",
    "mocha": "^5.0.4",
    "onml": "^0.4.0",
    "yargs": "^7.0.0"
  }
}
